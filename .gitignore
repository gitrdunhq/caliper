# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
.venv/

# Testing & coverage
.pytest_cache/
htmlcov/
.coverage
.mypy_cache/

# Linting & formatting
.ruff_cache/

# Environment & secrets
.env
.env.*
!.env.example

# macOS
.DS_Store
.AppleDouble
.LSOverride

# WFC pipeline state
.runs/
.temp/
.wfc/

# Serena tooling
.serena/

# Browser-gate screenshot artifacts (scripts/screenshots.ts output)
screenshots/

# Node (build toolchain for the `caliper part` web frontend, scripts/part_ui/
# and scripts/screenshots.ts — package.json + lockfile ARE committed for
# reproducible builds; only the installed tree is ignored)
node_modules/

# Runtime artifacts (generated, not committed)
evidence/
diagrams/
*.svg
!assets/*.svg
*.zip
.caliper/reports/
.caliper/code_graph.sqlite
.claude/worktrees/
.worktrees/

# Local infrastructure (never commit, never delete)
.dogfood/
.dogood/
docs/pitch-page/
.scratch/

# Sandbox container-test scratch (CA-injected build workaround — env-specific, never commit)
Dockerfile.ci-test
Dockerfile.test.ci
cabuild/
egress-gateway-ca-*.crt
swp-ca-*.crt
.tokensave
