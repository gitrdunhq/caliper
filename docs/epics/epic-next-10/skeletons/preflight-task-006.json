{
  "task_id": "r2-semgrep-severity-floor",
  "language": "python",
  "framework": "pytest",
  "outputs": [
    {
      "ac_id": "AC1",
      "path": "tests/unit/test_repo_config.py",
      "kind": "pytest",
      "purpose": "Verify repo_config parses thresholds.semgrep.min_severity from .cal",
      "property_id": "PROP-001",
      "function_name": "test_ac1_repo_config_parses_thresholds_semgrep_min_severity_from_cali",
      "skeleton_written": false,
      "content": "# Skeleton: r2-semgrep-severity-floor AC1 \u2014 PROP-001\n# RED agent: fill in the assertion body. Do not rename this function or move this file.\n# Traceability: AC1 \u2192 test_ac1_repo_config_parses_thresholds_semgrep_min_severity_from_cali \u2192 tests/unit/test_repo_config.py\n\nimport pytest\n\n\nclass Testr2_semgrep_severity_floor_AC1:\n    def test_ac1_repo_config_parses_thresholds_semgrep_min_severity_from_cali(self):\n        \"\"\"\n        PROP-001: repo_config parses thresholds.semgrep.min_severity from .caliper.yaml, defaultin\n        \"\"\"\n        # Arrange\n\n        # Act\n\n        # Assert \u2014 prove PROP-001\n        raise NotImplementedError(\"RED agent: implement this assertion\")\n",
      "implementation_required": true,
      "skeleton_note": "preflight-only: content emitted in JSON, not written to disk"
    },
    {
      "ac_id": "AC2",
      "path": "tests/unit/test_repo_config.py",
      "kind": "pytest",
      "purpose": "Verify a semgrep Finding with severity='low' or 'info' is excluded ",
      "property_id": "PROP-002",
      "function_name": "test_ac2_a_semgrep_finding_with_severity_low_or_info_is_excluded_from",
      "skeleton_written": false,
      "content": "# Skeleton: r2-semgrep-severity-floor AC2 \u2014 PROP-002\n# RED agent: fill in the assertion body. Do not rename this function or move this file.\n# Traceability: AC2 \u2192 test_ac2_a_semgrep_finding_with_severity_low_or_info_is_excluded_from \u2192 tests/unit/test_repo_config.py\n\nimport pytest\n\n\nclass Testr2_semgrep_severity_floor_AC2:\n    def test_ac2_a_semgrep_finding_with_severity_low_or_info_is_excluded_from(self):\n        \"\"\"\n        PROP-002: a semgrep Finding with severity='low' or 'info' is excluded from verdict/score c\n        \"\"\"\n        # Arrange\n\n        # Act\n\n        # Assert \u2014 prove PROP-002\n        raise NotImplementedError(\"RED agent: implement this assertion\")\n",
      "implementation_required": true,
      "skeleton_note": "preflight-only: content emitted in JSON, not written to disk"
    },
    {
      "ac_id": "AC3",
      "path": "tests/unit/test_repo_config.py",
      "kind": "pytest",
      "purpose": "Verify such below-floor semgrep findings are still returned and ren",
      "property_id": "PROP-003",
      "function_name": "test_ac3_such_below_floor_semgrep_findings_are_still_returned_and_ren",
      "skeleton_written": false,
      "content": "# Skeleton: r2-semgrep-severity-floor AC3 \u2014 PROP-003\n# RED agent: fill in the assertion body. Do not rename this function or move this file.\n# Traceability: AC3 \u2192 test_ac3_such_below_floor_semgrep_findings_are_still_returned_and_ren \u2192 tests/unit/test_repo_config.py\n\nimport pytest\n\n\nclass Testr2_semgrep_severity_floor_AC3:\n    def test_ac3_such_below_floor_semgrep_findings_are_still_returned_and_ren(self):\n        \"\"\"\n        PROP-003: such below-floor semgrep findings are still returned and rendered by renderer.py\n        \"\"\"\n        # Arrange\n\n        # Act\n\n        # Assert \u2014 prove PROP-003\n        raise NotImplementedError(\"RED agent: implement this assertion\")\n",
      "implementation_required": true,
      "skeleton_note": "preflight-only: content emitted in JSON, not written to disk"
    },
    {
      "ac_id": "AC4",
      "path": "tests/unit/test_repo_config.py",
      "kind": "pytest",
      "purpose": "Verify setting thresholds.semgrep.min_severity: 'low' in .caliper.y",
      "property_id": "PROP-004",
      "function_name": "test_ac4_setting_thresholds_semgrep_min_severity_low_in_caliper_yaml",
      "skeleton_written": false,
      "content": "# Skeleton: r2-semgrep-severity-floor AC4 \u2014 PROP-004\n# RED agent: fill in the assertion body. Do not rename this function or move this file.\n# Traceability: AC4 \u2192 test_ac4_setting_thresholds_semgrep_min_severity_low_in_caliper_yaml \u2192 tests/unit/test_repo_config.py\n\nimport pytest\n\n\nclass Testr2_semgrep_severity_floor_AC4:\n    def test_ac4_setting_thresholds_semgrep_min_severity_low_in_caliper_yaml(self):\n        \"\"\"\n        PROP-004: setting thresholds.semgrep.min_severity: 'low' in .caliper.yaml causes 'low' sev\n        \"\"\"\n        # Arrange\n\n        # Act\n\n        # Assert \u2014 prove PROP-004\n        raise NotImplementedError(\"RED agent: implement this assertion\")\n",
      "implementation_required": true,
      "skeleton_note": "preflight-only: content emitted in JSON, not written to disk"
    }
  ],
  "impl_stubs": [],
  "existing_api": {
    "src/caliper/core/repo_config.py": "# tested-by: tests/unit/test_repo_config.py\n# tested-by: tests/unit/test_repo_config_merge.py\n\nfrom __future__ import annotations\n\nfrom pathlib import Path\nfrom typing import Any\n\nimport structlog\nimport yaml\nfrom pydantic import (\n)\n\nfrom caliper.core.models import ChangeType, PartTarget\n\n# Structural buckets are facts from git, not classification guesses \u2014 an override\n# may not target them (a file is a delete/move/binary or it is not).\n_STRUCTURAL_BUCKETS: frozenset[ChangeType] = frozenset(\n)\n\nlogger = structlog.get_logger()\n\n_CONFIG_FILENAME = \".caliper.yaml\"\n\n\nclass SemgrepConfig(BaseModel):\n    ...\n\n\n\nclass PluginConfig(BaseModel):\n    ...\n\n\n\nclass DetectorsConfig(BaseModel):\n    ...\n\n\n\n# Default classification globs for parting. Matched (fnmatch-style) against the\n# posix relative path AND the basename, so both ``poetry.lock`` and\n# ``sub/dir/poetry.lock`` match. Order does not matter \u2014 classification in\n# ``part_stock`` checks generated, then config, then test, then falls to logic.\n_DEFAULT_GENERATED_GLOBS: list[str] = [\n]\n# Generic runtime/app config. Deliberately low-precedence: ``*.yaml`` is greedy,\n# so the specific buckets below (ci_cd, infra, schema_contracts, supply_chain) are\n# matched FIRST in ``part_stock._classify``. ``.github/**`` and ``Dockerfile`` used\n# to live here; they now route to ci_cd / infra respectively.\n_DEFAULT_CONFIG_GLOBS: list[str] = [\n]\n# Security & policy-as-code (Rego, IAM, policy bundles).\n_DEFAULT_SECURITY_POLICY_GLOBS: list[str] = [\n]\n# Dependency manifests (the human-edited source; lockfiles stay ``generated``).\n_DEFAULT_SUPPLY_CHAIN_GLOBS: list[str] = [\n]\n# CI/CD pipelines and build orchestration.\n_DEFAULT_CI_CD_GLOBS: list[str] = [\n]\n# Schemas, contracts, and migrations \u2014 the wire/storage shape.\n_DEFAULT_SCHEMA_CONTRACTS_GLOBS: list[str] = [\n]\n# Documentation and prose.\n_DEFAULT_DOCUMENTATION_GLOBS: list[str] = [\n]\n# Infrastructure-as-code and runtime/cloud topology.\n_DEFAULT_INFRA_GLOBS: list[str] = [\n]\n# Architectural code tiers that are NOT glob-determinable across repos\n# (frontend/data/business) default to empty: unmatched code falls to the\n# ``logic`` residual and is tiered by a human via the reclassify loop.\n_DEFAULT_FRONTEND_GLOBS: list[str] = []\n_DEFAULT_DATA_GLOBS: list[str] = []\n_DEFAULT_BUSINESS_GLOBS: list[str] = []\n_DEFAULT_TEST_GLOBS: list[str] = [\n]\n\n\nclass OverrideRule(BaseModel):\n    ...\n\n\n\n\n\n\nclass PartingConfig(BaseModel):\n    ...\n\n\n\n\nclass BaselineConfig(BaseModel):\n    ...\n\n\n\nclass ArchitectureConfig(BaseModel):\n    ...\n\n\n\n\nclass RepoConfig(BaseModel):\n    ...\n\n\n\ndef load_merged_config(repo_path: Path, package_root: Path | None = None) -> RepoConfig:\n    ...\n\n\n# (resolved config path) -> ((mtime_ns, size) or None when absent, parsed config).\n# Every plugin loads the repo config; read and log it once per run, not once\n# per plugin. Invalidated by mtime so an edited file is picked up.\n_CACHE: dict[Path, tuple[tuple[int, int] | None, RepoConfig]] = {}\n\n\ndef clear_repo_config_cache() -> None:\n    ...\n\n\ndef load_repo_config(repo_path: Path) -> RepoConfig:\n    ...\n\n\ndef _read_repo_config(config_file: Path, *, present: bool) -> RepoConfig:\n    ...\n\n\n\n\n\n",
    "src/caliper/core/review_summary.py": "# tested-by: tests/unit/test_review_summary.py\n\nfrom __future__ import annotations\n\nfrom enum import StrEnum\n\nfrom caliper._base import Contract\nfrom caliper.core.plugin import finding_get\n\n# Canonical severity -> SARIF-style level map. The one mapping the whole system uses\n# (SARIF imports this); unmapped severities fall back to \"note\" (least alarming).\nSEVERITY_TO_LEVEL: dict[str, str] = {\n}\n\n# Categories whose findings gate a merge (\"security blocks, quality advises\").\nSECURITY_CATEGORIES = frozenset({\"dependency\", \"supply_chain\", \"infra\"})\n\n\nclass ReviewVerdict(StrEnum):\n    ...\n\n\n\nclass ReviewSummary(Contract):\n    ...\n\n\n\ndef level_for(severity: object) -> str:\n    ...\n\n\ndef _norm(path: object) -> str:\n    ...\n\n\ndef summarize_review(\n    results: list,\n) -> ReviewSummary:\n    from caliper.core.renderer import calculate_quality_score, calculate_severity_score\n\n    changed = {_norm(f) for f in changed_files} if changed_files is not None else None\n\n    errors = warnings = notes = crashed = skipped = blocking = 0\n        is_security = str(getattr(r, \"category\", \"\") or \"\") in SECURITY_CATEGORIES\n            level = level_for(finding_get(finding, \"severity\"))\n            elif level == \"warning\":\n                file = finding_get(finding, \"file\")\n                attributable = changed is None or (bool(file) and _norm(file) in changed)\n\n        verdict = ReviewVerdict.blocked\n        verdict = ReviewVerdict.incomplete\n        verdict = ReviewVerdict.warnings\n        verdict = ReviewVerdict.clear\n\n        verdict=verdict,\n        error_count=errors,\n        warning_count=warnings,\n        note_count=notes,\n        crashed_count=crashed,\n        skipped_count=skipped,\n        blocking_count=blocking,\n        security_score=calculate_severity_score(results),\n        quality_score=calculate_quality_score(results),\n",
    "src/caliper/core/renderer.py": "\nfrom __future__ import annotations\n\nfrom dataclasses import replace\nfrom pathlib import Path\n\nimport jinja2\n\nfrom caliper.core.plugin import PluginResult, finding_as_dict, finding_get\nfrom caliper.core.version import get_version\n\n_MAX_COMMENT_LENGTH = 65536\n\nCATEGORY_PRIORITY: dict[str, int] = {\n}\n_DEFAULT_TEMPLATE_DIR = Path(__file__).parent.parent / \"templates\"\n_VERSION = get_version()\n\n_SEVERITY_WEIGHTS: dict[str, int] = {\n}\n\n_SECURITY_PLUGINS: set[str] = {\n}\n\n_QUALITY_PLUGINS: set[str] = {\n}\n\n\ndef _plugin_is_security(plugin_name: str) -> bool:\n    ...\n\n\ndef calculate_severity_score(results: list[PluginResult]) -> float:\n    ...\n\n\ndef calculate_quality_score(results: list[PluginResult]) -> float:\n    ...\n\n\ndef _is_monorepo(results: list[PluginResult]) -> bool:\n    ...\n\n\ndef _group_by_package(results: list[PluginResult]) -> dict[str, list[PluginResult]]:\n    ...\n\n\ndef _verdict_rank(verdict: str) -> int:\n    ...\n\n\ndef _build_monorepo_sections(\n    results: list[PluginResult],\n) -> tuple[str, list[tuple[str, str]], list[str]]:\n    packages = _group_by_package(results)\n    overall_verdict = \"clear\"\n    summary_rows: list[tuple[str, str]] = []\n    sections: list[str] = []\n\n        pkg_verdict, pkg_rows, pkg_finding_sections = _build_sections(pkg_results, plugin_renderers)\n        pkg_score = calculate_severity_score(pkg_results)\n        pkg_quality = calculate_quality_score(pkg_results)\n\n            overall_verdict = pkg_verdict\n\n        header = pkg_root if pkg_root else \"(root)\"\n        pkg_lines: list[str] = [\n\n\n\n\n\ndef render_comment(\n    results: list[PluginResult],\n    repo: str = \"\",\n    pr_num: int = 0,\n    title: str = \"\",\n    file_count: int = 0,\n) -> str:\n    results = [replace(r, findings=[finding_as_dict(f) for f in r.findings]) for r in results]\n    tpl_dir = template_dir or _DEFAULT_TEMPLATE_DIR\n    env = jinja2.Environment(\n        loader=jinja2.FileSystemLoader(str(tpl_dir)),\n        autoescape=False,  # nosemgrep: jinja2-autoescape-disabled \u2014 output is markdown, not HTML\n        keep_trailing_newline=True,\n    template = env.get_template(\"comment.md.j2\")\n\n        _, summary_rows, sections = _build_monorepo_sections(results, plugin_renderers)\n        _, summary_rows, sections = _build_sections(results, plugin_renderers)\n\n        from caliper.core.review_summary import summarize_review\n\n        verdict = summarize_review(results).verdict.value\n\n    severity_score = calculate_severity_score(results)\n    quality_score = calculate_quality_score(results)\n    mi_grade, mi_score, mi_icon, avg_ccn, hi_ccn, mi_c_count = _extract_mi(results)\n\n    from caliper.core.actionability import classify_findings\n\n    act_summary = classify_findings(results)\n\n    footer_parts = []\n        count = len(r.findings)\n    footer_stats = \" \u00b7 \".join(footer_parts) if footer_parts else \"clean\"\n\n    output = template.render(\n        repo=repo,\n        pr_num=pr_num,\n        title=title,\n        verdict=verdict,\n        file_count=file_count,\n        summary_rows=summary_rows,\n        sections=sections,\n        severity_score=severity_score,\n        quality_score=quality_score,\n        mi_grade=mi_grade,\n        mi_score=mi_score,\n        mi_icon=mi_icon,\n        avg_ccn=avg_ccn,\n        hi_ccn=hi_ccn,\n        mi_c_count=mi_c_count,\n        version=_VERSION,\n        footer_stats=footer_stats,\n        actionability=act_summary,\n\n        truncated = output[: _MAX_COMMENT_LENGTH - 100]\n\n\n\ndef _build_sections(\n    results: list[PluginResult],\n) -> tuple[str, list[tuple[str, str]], list[str]]:\n    verdict = \"clear\"\n    summary_rows: list[tuple[str, str]] = []\n    sections: list[str] = []\n\n    _max_priority = max(CATEGORY_PRIORITY.values()) + 1\n    sorted_results = sorted(\n        key=lambda r: CATEGORY_PRIORITY.get(r.category, _max_priority),\n\n        count = len(r.findings)\n        label = r.plugin_name\n\n                verdict = \"incomplete\"\n\n        status = r.summary.get(\"status\", \"\")\n\n\n        has_crit = any(finding_get(f, \"severity\") in (\"critical\", \"high\") for f in r.findings)\n        is_security = r.category in {\"dependency\", \"supply_chain\", \"infra\"}\n            verdict = \"blocked\"\n            verdict = \"warnings\"\n\n        renderer = None\n            renderer = plugin_renderers.get(r.plugin_name)\n\n                md = renderer.render(r)\n                md = _default_render(r)\n            md = _default_render(r)\n\n\n\n\ndef _default_render(result: PluginResult) -> str:\n    ...\n\n\nclass MarkdownRenderer:\n    ...\n\n\n\ndef _extract_mi(\n    results: list[PluginResult],\n) -> tuple[str, int, str, float, int, int]:\n            s = r.summary\n            avg = s.get(\"avg_cyclomatic_complexity\", 0)\n            mi_scores = []\n                mi_str = finding_get(f, \"maintainability_index\", \"\")\n                        val = float(str(mi_str).split(\"(\")[1].rstrip(\")\"))\n            avg_mi = sum(mi_scores) / len(mi_scores) if mi_scores else 0\n            grade = \"A\" if avg_mi >= 20 else (\"B\" if avg_mi >= 10 else \"C\")\n            icon = {\"A\": \"\ud83d\udfe2\", \"B\": \"\ud83d\udfe1\", \"C\": \"\ud83d\udd34\"}.get(grade, \"\u26aa\")\n            hi = s.get(\"high_complexity_count\", 0)\n            c_count = sum(1 for s in mi_scores if s < 10)\n\n\nfrom caliper.core.port_registries import RENDERERS  # noqa: E402  (registration wiring)\n\n\n@RENDERERS.register(\"markdown\")\ndef build_markdown_renderer() -> MarkdownRenderer:\n    ...\n"
  },
  "target_context": null,
  "no_skeletons_reason": null
}