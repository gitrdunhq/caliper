{
  "task_id": "r8-container-runner-flag",
  "language": "python",
  "framework": "pytest",
  "outputs": [
    {
      "ac_id": "AC1",
      "path": "tests/unit/test_review_cmd.py",
      "kind": "pytest",
      "purpose": "Verify 'caliper review --runner auto' with a fake ToolRunnerPort re",
      "property_id": "PROP-001",
      "function_name": "test_ac1_caliper_review_runner_auto_with_a_fake_toolrunnerport_report",
      "skeleton_written": false,
      "content": "# Skeleton: r8-container-runner-flag AC1 \u2014 PROP-001\n# RED agent: fill in the assertion body. Do not rename this function or move this file.\n# Traceability: AC1 \u2192 test_ac1_caliper_review_runner_auto_with_a_fake_toolrunnerport_report \u2192 tests/unit/test_review_cmd.py\n\nimport pytest\n\n\nclass Testr8_container_runner_flag_AC1:\n    def test_ac1_caliper_review_runner_auto_with_a_fake_toolrunnerport_report(self):\n        \"\"\"\n        PROP-001: 'caliper review --runner auto' with a fake ToolRunnerPort reporting podman prese\n        \"\"\"\n        # Arrange\n\n        # Act\n\n        # Assert \u2014 prove PROP-001\n        raise NotImplementedError(\"RED agent: implement this assertion\")\n",
      "implementation_required": true,
      "skeleton_note": "preflight-only: content emitted in JSON, not written to disk"
    },
    {
      "ac_id": "AC2",
      "path": "tests/unit/test_review_cmd.py",
      "kind": "pytest",
      "purpose": "Verify 'caliper review --runner container' invocation assembles a r",
      "property_id": "PROP-002",
      "function_name": "test_ac2_caliper_review_runner_container_invocation_assembles_a_run_c",
      "skeleton_written": false,
      "content": "# Skeleton: r8-container-runner-flag AC2 \u2014 PROP-002\n# RED agent: fill in the assertion body. Do not rename this function or move this file.\n# Traceability: AC2 \u2192 test_ac2_caliper_review_runner_container_invocation_assembles_a_run_c \u2192 tests/unit/test_review_cmd.py\n\nimport pytest\n\n\nclass Testr8_container_runner_flag_AC2:\n    def test_ac2_caliper_review_runner_container_invocation_assembles_a_run_c(self):\n        \"\"\"\n        PROP-002: 'caliper review --runner container' invocation assembles a run command mounting \n        \"\"\"\n        # Arrange\n\n        # Act\n\n        # Assert \u2014 prove PROP-002\n        raise NotImplementedError(\"RED agent: implement this assertion\")\n",
      "implementation_required": true,
      "skeleton_note": "preflight-only: content emitted in JSON, not written to disk"
    },
    {
      "ac_id": "AC3",
      "path": "tests/unit/test_review_cmd.py",
      "kind": "pytest",
      "purpose": "Verify the container invocation runs as the image's non-root user a",
      "property_id": "PROP-003",
      "function_name": "test_ac3_the_container_invocation_runs_as_the_image_s_non_root_user_a",
      "skeleton_written": false,
      "content": "# Skeleton: r8-container-runner-flag AC3 \u2014 PROP-003\n# RED agent: fill in the assertion body. Do not rename this function or move this file.\n# Traceability: AC3 \u2192 test_ac3_the_container_invocation_runs_as_the_image_s_non_root_user_a \u2192 tests/unit/test_review_cmd.py\n\nimport pytest\n\n\nclass Testr8_container_runner_flag_AC3:\n    def test_ac3_the_container_invocation_runs_as_the_image_s_non_root_user_a(self):\n        \"\"\"\n        PROP-003: the container invocation runs as the image's non-root user and the CLI returns t\n        \"\"\"\n        # Arrange\n\n        # Act\n\n        # Assert \u2014 prove PROP-003\n        raise NotImplementedError(\"RED agent: implement this assertion\")\n",
      "implementation_required": true,
      "skeleton_note": "preflight-only: content emitted in JSON, not written to disk"
    },
    {
      "ac_id": "AC4",
      "path": "tests/unit/test_review_cmd.py",
      "kind": "pytest",
      "purpose": "Verify 'caliper part' does not accept or is unaffected by --runner ",
      "property_id": "PROP-004",
      "function_name": "test_ac4_caliper_part_does_not_accept_or_is_unaffected_by_runner_and",
      "skeleton_written": false,
      "content": "# Skeleton: r8-container-runner-flag AC4 \u2014 PROP-004\n# RED agent: fill in the assertion body. Do not rename this function or move this file.\n# Traceability: AC4 \u2192 test_ac4_caliper_part_does_not_accept_or_is_unaffected_by_runner_and \u2192 tests/unit/test_review_cmd.py\n\nimport pytest\n\n\nclass Testr8_container_runner_flag_AC4:\n    def test_ac4_caliper_part_does_not_accept_or_is_unaffected_by_runner_and(self):\n        \"\"\"\n        PROP-004: 'caliper part' does not accept or is unaffected by --runner and always executes \n        \"\"\"\n        # Arrange\n\n        # Act\n\n        # Assert \u2014 prove PROP-004\n        raise NotImplementedError(\"RED agent: implement this assertion\")\n",
      "implementation_required": true,
      "skeleton_note": "preflight-only: content emitted in JSON, not written to disk"
    },
    {
      "ac_id": "AC5",
      "path": "tests/unit/test_review_cmd.py",
      "kind": "pytest",
      "purpose": "Verify all of the above are tested through a fake ToolRunnerPort; n",
      "property_id": "PROP-005",
      "function_name": "test_ac5_all_of_the_above_are_tested_through_a_fake_toolrunnerport_no",
      "skeleton_written": false,
      "content": "# Skeleton: r8-container-runner-flag AC5 \u2014 PROP-005\n# RED agent: fill in the assertion body. Do not rename this function or move this file.\n# Traceability: AC5 \u2192 test_ac5_all_of_the_above_are_tested_through_a_fake_toolrunnerport_no \u2192 tests/unit/test_review_cmd.py\n\nimport pytest\n\n\nclass Testr8_container_runner_flag_AC5:\n    def test_ac5_all_of_the_above_are_tested_through_a_fake_toolrunnerport_no(self):\n        \"\"\"\n        PROP-005: all of the above are tested through a fake ToolRunnerPort; no test spawns a real\n        \"\"\"\n        # Arrange\n\n        # Act\n\n        # Assert \u2014 prove PROP-005\n        raise NotImplementedError(\"RED agent: implement this assertion\")\n",
      "implementation_required": true,
      "skeleton_note": "preflight-only: content emitted in JSON, not written to disk"
    }
  ],
  "impl_stubs": [],
  "existing_api": {
    "src/caliper/cli/main.py": "\n# tested-by: tests/unit/test_cli.py\n\nfrom __future__ import annotations\n\nimport os\nimport re\nimport sys\nfrom pathlib import Path\n\nimport click\nimport structlog\n\nfrom caliper.cli.cli_shared import (  # noqa: F401\n)\nfrom caliper.cli.watch import _IGNORE_DIRS, _WATCH_EXTENSIONS, DebounceTimer  # noqa: F401\nfrom caliper.core.models import OperatingMode\nfrom caliper.plugins import get_default_registry\n\nlogger = structlog.get_logger()\n\n_ALLOWED_TEAMS: frozenset[str] = frozenset(\n)\n\n\ndef _validate_repo_path(ctx: click.Context, param: click.Parameter, value: str) -> str:\n    ...\n\n\ndef _validate_pr_url(ctx: click.Context, param: click.Parameter, value: str) -> str:\n    ...\n\n\ndef _validate_team(ctx: click.Context, param: click.Parameter, value: str) -> str:\n    ...\n\n\ndef _validate_gh_repo(ctx: click.Context, param: click.Parameter, value: str | None) -> str | None:\n    ...\n\n\ndef _is_isolated_environment() -> bool:\n    ...\n\n\ndef _check_isolated_environment() -> None:\n    ...\n\n\n@click.group()\n@click.version_option(package_name=\"caliper\")\n@click.option(\"-v\", \"--verbose\", is_flag=True, help=\"Debug logging (or CALIPER_LOG_LEVEL=debug).\")\ndef cli(verbose: bool) -> None:\n    ...\n\n\n\n\ndef _register_subcommands() -> None:\n    ...\n\n\n\n_register_subcommands()\n\n\n@cli.command()\n@click.option(\n    required=True,\n    type=click.Path(),\n    callback=_validate_repo_path,\n    help=\"Path to the repository root.\",\n)\n@click.option(\"--diff\", required=True, type=str, help=\"Path to diff file, or '-' for stdin.\")\n@click.option(\n    required=False,\n    default=None,\n    type=str,\n    callback=_validate_pr_url,\n    help=\"PR URL for context and comments. Optional; omit for local/non-PR runs.\",\n)\n@click.option(\n    required=False,\n    default=\"unknown\",\n    type=str,\n    callback=_validate_team,\n    help='Team name submitting the request. Defaults to \"unknown\" when omitted.',\n)\n@click.option(\n    required=True,\n    type=click.Choice([\"monitor\", \"advise\"]),\n    help=\"Operating mode.\",\n)\n@click.option(\n    type=str,\n    default=None,\n    help=(\n)\ndef evaluate(\n    repo_path: str,\n) -> None:\n    diff_text = _read_diff(diff)\n    mode = OperatingMode(operating_mode)\n\n        from caliper.core.config import CaliperSettings\n\n        config = CaliperSettings()  # type: ignore[call-arg]\n            \"config_load_failed\", msg=\"Pipeline skipped \u2014 config unavailable (fail-open)\"\n\n        import orjson\n\n        from caliper.composition.bootstrap import bootstrap as _bootstrap\n        from caliper.core.pipeline import ReviewPipeline\n\n        _context = _bootstrap(config)\n        pipeline = ReviewPipeline(config, context=_context)\n        decisions = pipeline.evaluate(\n            diff_text=diff_text,\n            pr_url=pr_url,\n            team=team,\n            mode=mode,\n            repo_path=Path(repo_path),\n\n        stdout_json = output_json == \"-\"\n\n\n\n            last = decisions[-1]\n            payload = orjson.dumps(last.model_dump(mode=\"json\"), option=orjson.OPT_INDENT_2)\n                p = Path(output_json)\n\n\n\n\n@cli.command()\n@click.option(\n    type=click.Choice([\"repo\", \"diff\", \"folder\"]),\n    default=None,\n    help=\"Scan scope: repo (full), diff (changed files only), folder (single directory).\",\n)\n@click.option(\"--diff\", type=str, default=None, help=\"Path to diff file.\")\n@click.option(\"--repo-path\", type=click.Path(exists=True), default=\".\", help=\"Repository root.\")\n@click.option(\"--scanners\", type=str, default=None, help=\"Comma-separated plugin names.\")\n@click.option(\"--category\", type=str, default=None, help=\"Comma-separated categories.\")\n@click.option(\n    is_flag=True,\n    hidden=True,\n    help=\"Deprecated no-op: a bare `caliper review` already runs every default plugin.\",\n)\n@click.option(\"--output\", type=click.Path(), default=None, help=\"Write output to file.\")\n@click.option(\n    is_flag=True,\n    help=\"Also scan test code (tests/, test_*.py, *.test.ts, ...); skipped by default.\",\n)\n@click.option(\n    type=click.Choice([\"markdown\", \"sarif\", \"json\", \"vex\"]),\n    default=\"markdown\",\n    help=\"Output format.\",\n)\n@click.option(\n    type=int,\n    default=1000,\n    help=\"Max findings per plugin in SARIF output. 0 for no limit.\",\n)\n@click.option(\"--pr-url\", type=str, default=\"\", help=\"PR URL for comment header.\")\n@click.option(\"--pr-num\", type=int, default=0, help=\"PR number.\")\n@click.option(\"--title\", type=str, default=\"PR Review\", help=\"PR title.\")\n@click.option(\n    is_flag=True,\n    help=\"Watch for file changes and re-run review (debounced 500 ms).\",\n)\n@click.option(\n    type=str,\n    default=\"\",\n    help=\"Comma-separated plugin names to disable.\",\n)\n@click.option(\n    type=str,\n    default=\"\",\n    help=\"Comma-separated plugin names to force-enable (overrides --disable).\",\n)\n@click.option(\n    type=click.Path(),\n    default=None,\n    help=\"Scan only this package directory.\",\n)\n@click.option(\n    type=click.IntRange(min=1),\n    default=None,\n    help=\"Post findings as inline PR review comments via GitHub API. Requires gh CLI.\",\n)\n@click.option(\n    type=str,\n    default=None,\n    callback=_validate_gh_repo,\n    is_eager=False,\n    help=\"GitHub repo (owner/name) for --pr mode. Auto-detected if omitted.\",\n)\ndef review(\n    scope: str | None,\n) -> None:\n    from caliper.cli.review_cmd import (\n    from caliper.composition.bootstrap import bootstrap_review\n    from caliper.core.plugin import PluginCategory\n    from caliper.core.repo_config import RepoConfig, load_repo_config\n    from caliper.core.use_cases import ScanScope\n\n    resolved_scope = ScanScope(scope) if scope else None\n\n    _ctx = bootstrap_review(registry_factory=get_default_registry)\n    registry = _ctx.analyzer_registry\n    repo = Path(repo_path)\n    names = scanners.split(\",\") if scanners else None\n    cats = [PluginCategory(c.strip()) for c in category.split(\",\")] if category else None\n    plugin_map = {p.name: p for p in registry.list()}\n    repo_name = pr_url.split(\"github.com/\")[-1].split(\"/pull\")[0] if \"github.com\" in pr_url else \"\"\n\n    repo_config = load_repo_config(repo) if (repo / \".caliper.yaml\").exists() else RepoConfig()\n    disabled_names, enabled_names = resolve_plugin_selection(\n        repo_config, disable=disable, enable=enable\n\n    def run_review() -> None:\n        ...\n\n\n\n\n\n\n        from caliper.cli.watch import watch_and_rerun\n\n\n\n",
    "src/caliper/cli/review_cmd.py": "\nfrom __future__ import annotations\n\nimport sys\nfrom collections.abc import Callable\nfrom pathlib import Path\nfrom typing import TYPE_CHECKING\n\nimport click\n\nfrom caliper.core.repo_config import RepoConfig\nfrom caliper.core.use_cases import ScanScope\n\nif TYPE_CHECKING:\n    from caliper.core.review_summary import ReviewSummary\n\n\n# Plugins that are expensive or noisy enough that they should never run\n# just because a repo config or --disable list happens to be silent about\n# them. Off by default; --enable <name> (or repo config plugins.enabled)\n# overrides per run_all's disabled/enabled precedence rule.\nDEFAULT_OPT_IN_PLUGINS: frozenset[str] = frozenset({\"scancode\"})\n\n\ndef resolve_plugin_selection(\n    repo_config: RepoConfig, *, disable: str, enable: str\n) -> tuple[set[str], set[str]]:\n    disabled_names: set[str] = set(DEFAULT_OPT_IN_PLUGINS)\n\n    enabled_names: set[str] = set(repo_config.plugins.enabled or [])\n\n\n\ndef apply_include_tests(include_tests: bool) -> None:\n    ...\n\n\n\ndef build_file_lists(\n    *,\n) -> tuple[list[str], list[str] | None]:\n\n    def _all_repo_files() -> list[str]:\n        ...\n\n    def _diff_files() -> list[str]:\n        ...\n\n\n        folder = Path(package).resolve()  # type: ignore[arg-type]\n\n\ndef render_review_output(\n    *,\n) -> None:\n        import orjson\n\n        from caliper.core.sarif import to_sarif\n\n        sarif_doc = to_sarif(\n            repo_path=str(repo),\n            max_findings_per_run=sarif_max_findings,\n            summary=summary,\n\n            from caliper.core.pr_review import (\n\n            target_repo = gh_repo or detect_gh_repo()\n\n                diff_files = get_pr_diff_files(target_repo, pr)\n            pr_review = sarif_to_review(sarif_doc, diff_files)\n            ok = post_review(target_repo, pr, pr_review)\n\n        sarif_text = orjson.dumps(sarif_doc, option=orjson.OPT_INDENT_2).decode()\n\n        from caliper.core.json_report import render_json\n\n        json_text = render_json(results, repo=repo_name or str(repo), summary=summary)\n\n        import orjson\n\n        from caliper.core.vex import to_vex\n\n        vex_text = orjson.dumps(to_vex(results), option=orjson.OPT_INDENT_2).decode()\n\n    from caliper.core.renderer import render_comment\n\n    md = render_comment(\n        repo=repo_name or str(repo),\n        pr_num=pr_num,\n        title=title,\n        file_count=file_count,\n        plugin_renderers=plugin_map,\n        verdict=summary.verdict.value if summary else None,\n",
    "src/caliper/composition/bootstrap.py": "# tested-by: tests/unit/test_bootstrap_wiring.py\n\nfrom __future__ import annotations\n\nfrom pathlib import Path\nfrom typing import TYPE_CHECKING\n\nfrom caliper.composition.scribes import build_scribes\nfrom caliper.core.context import ApplicationContext\nfrom caliper.core.port_registries import (\n)\nfrom caliper.core.ports import (\n)\nfrom caliper.core.tool_runner import ToolInvocation, ToolResult\n\nif TYPE_CHECKING:\n    from caliper.core.config import CaliperSettings\n\n__all__ = [\n]\n\n\n# ---------------------------------------------------------------------------\n# Fake implementations for bootstrap_test() and bootstrap_review()\n# ---------------------------------------------------------------------------\n\n\nclass _FakeAnalyzerRegistry:\n    ...\n\n\n\n\nclass _FakePackageIndex:\n    ...\n\n\n\nclass _FakeToolRunner:\n    ...\n\n\n\n# ---------------------------------------------------------------------------\n# bootstrap_test()\n# ---------------------------------------------------------------------------\n\n\ndef bootstrap_test() -> ApplicationContext:\n    ...\n\n\n\n# ---------------------------------------------------------------------------\n# bootstrap_review() \u2014 minimal context for plugin review command\n# ---------------------------------------------------------------------------\n\n\ndef bootstrap_review(registry_factory=None) -> ApplicationContext:\n    ...\n\n\n\n\n\n# ---------------------------------------------------------------------------\n# Production adapter helpers \u2014 keep Null* instantiation out of bootstrap()\n# ---------------------------------------------------------------------------\n\n\ndef build_decision_store(settings: CaliperSettings) -> DecisionStorePort:\n    ...\n\n\n\n\ndef build_scan_cache(settings: CaliperSettings) -> ScanCachePort:\n    ...\n\n\n\ndef build_audit_sink(settings: CaliperSettings) -> AuditSinkPort:\n    ...\n\n\n\n\n\ndef build_publisher(settings: CaliperSettings) -> PullRequestPublisherPort:\n    ...\n\n\n\ndef build_package_index(settings: CaliperSettings):\n    ...\n\n\n# ---------------------------------------------------------------------------\n# Review-pipeline collaborators \u2014 supplied to the pipeline via ApplicationContext\n# ---------------------------------------------------------------------------\n\n# config.enabled_scanners uses the analyzer-style names; map them to SCANNERS keys.\n_SCANNER_REGISTRY_KEYS = {\n}\n\n\ndef run_supply_chain_scan(diff_text: str, settings: CaliperSettings, *, sources=None) -> list:\n    ...\n\n\n\ndef build_default_codegraph_factory():\n    ...\n\n\n\n\n\n\n\ndef build_grounding_provider(settings: CaliperSettings) -> GroundingProviderPort:\n    ...\n\n\n\n\n\n\ndef run_grounding(files: list[str], settings: CaliperSettings, *, root: str | None = None) -> dict:\n    ...\n\n\n\n\ndef build_scanners(settings: CaliperSettings) -> list:\n    ...\n\n\n\ndef build_evidence_writer(settings: CaliperSettings):\n    ...\n\n\n\ndef build_package_metadata(settings: CaliperSettings):\n    ...\n\n\ndef build_decision_repository(settings: CaliperSettings):\n    ...\n\n\n\n\ndef build_audit_log_appender():\n    ...\n\n\n\n# Backward-compatible aliases. The epic renames `_make_*` -> `build_*`; these\n# keep existing imports and inspect-based wiring guards working unchanged.\n_make_decision_store = build_decision_store\n_make_audit_sink = build_audit_sink\n_make_publisher = build_publisher\n_make_package_index = build_package_index\n\n\n# ---------------------------------------------------------------------------\n# bootstrap(settings)\n# ---------------------------------------------------------------------------\n\n\ndef load_adapters() -> None:\n    ...\n\n\ndef bootstrap(settings: CaliperSettings) -> ApplicationContext:\n    ...\n\n\n\n\n\n"
  },
  "target_context": null,
  "no_skeletons_reason": null
}