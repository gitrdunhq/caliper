plugins:
  disabled:
    - mypy
    - blast-radius
  semgrep:
    exclude_rules:
      # caliper is Python 3.12+ — py3.7 backwards-compat rules are inapplicable
      - python37-compatibility-importlib2
